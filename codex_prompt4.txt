- change-id: simulate-4n-passk-validation-thresholds
- Implement ONLY this task (no other tasks, no refactors outside scope):
- [ ] 1. 在 `generate_11n_dataset.py` 设计 4N 模拟输出模式 [#R1]
  - ACCEPT: 每题可稳定产出 4 个候选；默认变换集合为 2 种变量名更改 + 2 种死代码插入；并保留 pass@k 所需关键字段。
  - TEST: Manual: 检查输出 JSONL 的 `candidates` 长度为 4，且含 `variant_type` 与问题 ID 字段。

- Follow `openspec/project.md` → `## tasks.md Checklist Format` → `### Validation bundle requirements (mandatory)`
- Produce a human-reproducible validation bundle under:
  `auto_test_openspec/simulate-4n-passk-validation-thresholds/run-4__task-1__ref-R1__<YYYYMMDDThhmmssZ>/`
- Worker MAY run quick local checks to ensure the bundle is runnable,
  but MUST NOT claim PASS/FAIL/validated (Supervisor is the final verifier).

A) Worker deliverables (validation bundle assets)
- Create a NEW run-folder (append-only; never overwrite prior runs):
  `auto_test_openspec/simulate-4n-passk-validation-thresholds/run-4__task-1__ref-R1__<YYYYMMDDThhmmssZ>/`
- Minimum required files inside the run-folder:
  - `task.md` (self-sufficient README; includes How-to-run + machine-decidable pass/fail criteria)
  - `run.sh` and `run.bat`
  - `logs/worker_startup.txt` (MANDATORY; see Startup ritual below)
  - `logs/` (for provenance + transcripts; keep append-only within this run folder)
  - `outputs/`

A) Startup ritual (MANDATORY, before any edits)
- REQUIRE CodeX STARTUP RITUAL:
  - read `openspec/changes/simulate-4n-passk-validation-thresholds/progress.txt`
  - read `openspec/changes/simulate-4n-passk-validation-thresholds/feature_list.json`
  - run `git log --oneline -20`
  - capture `GIT_BASE` via `git rev-parse --short HEAD`
  - write a Startup snapshot to the validation bundle (NOT tasks.md), at:
    - `auto_test_openspec/simulate-4n-passk-validation-thresholds/<run-folder>/logs/worker_startup.txt`
  - The snapshot MUST include: UTC timestamp, CODEX_CMD, GIT_BASE, the git-log excerpt, and a short "what I observed" summary.

B) tasks.md bookkeeping (Worker-owned; single-line; NO conclusions)
- require Codex to update `openspec/changes/simulate-4n-passk-validation-thresholds/tasks.md` under THIS task with exactly ONE Worker bookkeeping line (NOT EVIDENCE):
  - starting with: `BUNDLE (RUN #4): ...`
  - MUST be a SINGLE LINE
  - MUST NOT write any `EVIDENCE (RUN #...)` line
  - MUST NOT write any PASS/FAIL/RESULT/validated= conclusions
- The single BUNDLE line MUST include ONLY:
  - `CODEX_CMD=codex exec --full-auto --skip-git-repo-check --model gpt-5.2-codex -c model_reasoning_effort=medium`
  - `SCOPE: CLI`
  - `VALIDATION_BUNDLE: auto_test_openspec/simulate-4n-passk-validation-thresholds/run-4__task-1__ref-R1__<YYYYMMDDThhmmssZ>`
  - `HOW_TO_RUN: run.sh/run.bat`
- forbid Codex from toggling ANY checkbox in tasks.

C) Governance boundaries (Worker forbidden; Supervisor-only)
- feature_list governance (MANDATORY; strict):
  - The Worker/Codex is FORBIDDEN to edit `openspec/changes/simulate-4n-passk-validation-thresholds/feature_list.json`
- forbid touching any other tasks
- The Worker/Codex is FORBIDDEN to create git commits.
